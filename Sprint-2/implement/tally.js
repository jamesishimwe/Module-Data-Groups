function tally(array) {
    const tally = {};
    if(!Array.isArray(array))
        throw new TypeError("Expected an array");
    if(array.length === 0)
    return tally;
    

}

module.exports = tally;
